spring:
  application:
    name: user-client
  profiles:
    active: dev

management: 
  endpoints:
    web: 
      exposure: 
        include: '*'
    jmx:
      exposure:
        include: '*'
  endpoint: 
    shutdown:
      enabled: true
  metrics:
    web:
      server:
        auto-time-requests: true