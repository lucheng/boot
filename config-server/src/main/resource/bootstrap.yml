spring:
  application:
    name: config-server
  profiles:
    active: native,dev
  cloud:
      config:
        server:
          native:
            search-locations: classpath:/config-files/
        fail-fast: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: true
  metrics:
    web:
      server:
        auto-time-requests: true
        
---
spring:
  profiles: native
  cloud:
      config:
        server:
          native:
            search-locations: classpath:/config-files/
        fail-fast: true