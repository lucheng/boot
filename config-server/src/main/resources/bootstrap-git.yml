spring:
  profiles: git
  cloud:
      config:
        server:
          git:
            default-label: master
            uri: https://github.com/lucheng/codegen
            search-paths:
            - /respo
            username:
            password:
        label: master
        fail-fast: true